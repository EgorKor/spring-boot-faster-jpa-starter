name: Simple Java Build
on: [push]

jobs:
  build:
    runs-on: linux_amd64
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Build and Test
        run: |
          sudo apt-get update
          sudo apt-get install -y openjdk-21-jdk maven
          mvn -B clean package